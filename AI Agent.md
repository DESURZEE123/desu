可以把它理解成一个 **“会调用业务系统的 AI 助手”**。技术上不是单纯接一个 ChatGPT 接口，而是要把 **大模型、业务数据、权限、系统 API、任务编排、结果校验** 串起来。

**一、它用到的核心技术知识**

**1. 大模型与 Prompt Engineering**

你需要懂大模型怎么理解自然语言指令。

比如用户说：

> 帮我查一下上周上海地区新增的未分配线索

AI 要识别出：

- 意图：查询线索
- 对象：线索 Lead
- 时间：上周
- 地区：上海
- 状态：未分配
- 动作：查询并返回列表

这里会用到：

- Prompt 设计
- 意图识别
- 槽位提取
- 多轮对话
- 上下文记忆
- 输出结构化 JSON

核心不是让 AI 写一段话，而是让 AI 把用户的话转成系统能执行的结构化指令。

例如：

```json
{
  "intent": "query_leads",
  "filters": {
    "created_at": "last_week",
    "region": "上海",
    "owner": null
  }
}
```

**2. Function Calling / Tool Calling**

这是 AI Agent 的关键。

大模型本身不会真的查数据库、分配线索、创建客户。它需要调用你提前定义好的工具。

比如你定义几个工具：

```text
query_leads()
assign_leads()
create_customer()
create_opportunity()
analyze_campaign_roi()
```

用户说一句话后，AI 判断应该调用哪个工具，并传入参数。

比如：

> 把今天新增的制造业线索分配给销售王晶

AI 调用：

```json
{
  "tool": "assign_leads",
  "arguments": {
    "created_at": "today",
    "industry": "制造业",
    "owner": "王晶"
  }
}
```

这就是 MarketUP AI Agent 最核心的技术能力。

**3. 业务对象建模**

你要搭建这种系统，必须先理解业务数据结构。

常见对象包括：

- Lead：线索
- Account：客户
- Contact：联系人
- Opportunity：商机
- Campaign：营销活动
- Content：内容
- Contract：合同
- User：销售/市场人员
- Task：跟进任务

每个对象要有字段和状态。

例如线索：

```text
线索 ID
姓名
手机号
公司
行业
地区
来源渠道
线索状态
负责人
创建时间
最近互动时间
评分
```

AI 要能操作系统，前提是这些业务对象足够清晰。

**4. API 设计与系统集成**

AI 不能直接改数据库，最好通过后端 API 操作业务系统。

比如：

```http
GET /api/leads
POST /api/leads/assign
POST /api/customers
POST /api/opportunities
GET /api/campaigns/{id}/roi
```

AI Agent 调用的是这些 API。

这样做的好处是：

- 权限可控；
- 操作可审计；
- 不容易误改数据；
- 可以复用原有业务逻辑；
- 方便限制 AI 能做什么、不能做什么。

**5. RAG / 企业知识库**

如果你想让 AI 回答产品资料、客户案例、行业知识，就需要 RAG。

RAG 简单说就是：

> 用户提问 → 系统去知识库检索相关资料 → 把资料给大模型 → 大模型基于资料回答。

适合放入知识库的内容：

- 产品手册；
- FAQ；
- 客户案例；
- 白皮书；
- 行业报告；
- 销售话术；
- 竞品对比；
- 活动资料；
- 官网文章。

技术上会用到：

- 文档切分 Chunking
- 向量化 Embedding
- 向量数据库
- 相似度检索
- Rerank
- 引用来源
- 防幻觉提示词

**6. 工作流编排**

有些任务不是调用一个 API 就结束，而是多步骤。

比如：

> 查找上周 SEM 渠道来的上海制造业线索，筛选评分超过 70 的，分配给王晶，并生成一份跟进建议。

背后流程是：

1. 查询线索；
2. 过滤渠道；
3. 过滤地区；
4. 过滤行业；
5. 过滤评分；
6. 分配负责人；
7. 生成跟进建议；
8. 返回结果。

这就需要工作流编排。

可以用：

- LangChain
- LlamaIndex
- Dify
- Coze / 扣子
- FastGPT
- Flowise
- 自研 Agent Orchestrator

如果你是产品经理，不一定要自己写完整代码，但要能画清楚流程。

**7. 权限、安全与审计**

ToB SaaS 的 AI Agent 不能想干嘛就干嘛。

必须考虑：

- 谁能查哪些客户；
- 谁能分配线索；
- 谁能修改商机金额；
- 哪些操作需要二次确认；
- AI 操作是否记录日志；
- 错误操作如何回滚；
- 敏感字段是否脱敏；
- 客户数据是否进入外部模型。

比如：

查询类操作可以直接执行；  
创建草稿类操作可以直接执行；  
删除、批量分配、修改金额、导出客户数据，必须二次确认。

这是 ToB AI 产品非常重要的专业点。

**二、如果你想自己搭建一个，建议这样做**

你可以从一个最小可用版本开始，不要一上来做完整 MarketUP。

**第一阶段：做一个“AI 查线索助手”**

目标：用户可以用自然语言查线索。

你需要准备：

- 一个线索表；
- 一个后端查询接口；
- 一个大模型接口；
- 一个简单聊天界面。

示例能力：

> 查一下今天新增的线索  
> 查一下上海地区制造业线索  
> 查一下还没有负责人的线索  
> 查一下评分大于 80 的高意向线索

技术链路：

```text
用户输入
↓
大模型识别意图和筛选条件
↓
生成结构化查询参数
↓
调用后端 API 查询线索
↓
返回结构化结果
↓
大模型总结成人话
```

**第二阶段：加入“AI 分配线索”**

目标：AI 不只查询，还能执行动作。

示例能力：

> 把今天新增的上海线索分配给王晶  
> 把评分超过 80 的未分配线索分给销售一组  
> 把制造业线索分配给负责制造业的销售

这里要加入：

- 销售人员表；
- 分配规则；
- 二次确认；
- 操作日志。

流程：

```text
用户输入
↓
AI 识别任务
↓
查询符合条件的线索
↓
展示待分配数量
↓
用户确认
↓
调用分配 API
↓
写入操作日志
↓
返回执行结果
```

**第三阶段：加入知识库**

目标：AI 可以基于企业资料回答问题、推荐资料。

示例能力：

> 给这个制造业客户推荐一份合适的案例  
> 根据客户关注点，生成一段跟进话术  
> 根据产品资料，生成一封销售跟进邮件

你需要：

- 上传产品资料；
- 文档切分；
- 建向量库；
- 检索相关内容；
- 让大模型基于检索结果生成答案。

**第四阶段：加入活动和内容分析**

目标：让 AI 做简单业务分析。

示例能力：

> 分析一下上周直播活动效果  
> 哪个渠道带来的线索质量最高？  
> 这篇白皮书下载后转化了多少商机？  
> 生成一份活动 ROI 复盘

你需要打通：

- 活动报名数据；
- 签到数据；
- 内容阅读数据；
- 线索数据；
- 商机数据；
- 成交数据。

**三、推荐技术栈**

如果你想自己从 0 到 1 做一个 Demo，可以用这套：

- 前端：React / Vue
- 后端：Python FastAPI 或 Node.js NestJS
- 数据库：PostgreSQL
- 向量库：pgvector / Milvus / Qdrant
- 大模型：OpenAI / 通义千问 / DeepSeek / 豆包
- Agent 框架：LangChain / LlamaIndex / Dify
- 权限系统：RBAC
- 日志系统：操作日志 + AI 调用日志

如果你是产品经理，建议先用低代码工具跑通：

- Dify：适合做 AI 应用和工作流；
- FastGPT：适合做知识库问答；
- Coze / 扣子：适合做 Agent 原型；
- 飞书多维表格 / Airtable：适合模拟业务数据库；
- Retool：适合快速搭管理后台。

**四、你作为产品经理要重点掌握什么**

不需要一开始就会写完整系统，但你要能讲清楚这些东西：

1. 用户一句话怎么被 AI 理解成业务动作；
2. AI 可以调用哪些系统能力；
3. 每个业务对象有哪些字段；
4. 哪些操作可以自动执行，哪些必须确认；
5. AI 生成结果如何校验；
6. 数据权限怎么控制；
7. 操作失败怎么提示和补救；
8. 业务效果怎么衡量。

面试里说这类项目，最专业的表达是：

> 我不是把 AI 当成一个聊天入口，而是把它设计成连接业务对象、系统 API 和用户任务的智能执行层。核心难点在于意图识别、业务对象映射、跨模块任务编排、权限控制和执行结果校验。

**五、最小 Demo 可以这样设计**

做一个「AI 线索助手」就够了。

包含 5 个功能：

- 自然语言查询线索；
- 自动筛选高意向线索；
- 分配线索给销售；
- 基于客户信息生成跟进建议；
- 记录 AI 操作日志。

这个 Demo 已经能体现 AI Agent 的核心能力：

```text
自然语言交互
业务对象识别
API 调用
权限控制
执行确认
结果反馈
```

这比做一个“AI 帮我写营销文案”的 Demo 专业很多，也更像 ToB SaaS 产品经理会做的东西。